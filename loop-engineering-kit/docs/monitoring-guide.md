# Monitoring and Observability

Track runs, successes, failures, escalations, iteration counts, latency, validation scores, token/tool costs, cache hit rate, and fallback frequency.

Use correlation IDs rather than raw prompts in general logs. Store sensitive traces only in access-controlled systems with retention limits. Alert on rising failures, iterations, latency, cost, and human-review queues.
